// Single source of truth for category options — used by both the
// create-post form and the home feed filter, so they never drift apart.
export const CATEGORIES = ["General", "Tech", "Career", "Random"];